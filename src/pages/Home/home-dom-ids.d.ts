declare global {
  const settingsTabButton: HTMLElement;
  const settings: HTMLElement;
  const settingsclose: HTMLElement;
  const mainCardsContainer: HTMLElement;
  const saveBtn: HTMLElement;
  const language2: HTMLElement;
  const themeOption: HTMLElement;
  const aboutCard: HTMLElement;
  const linksCard: HTMLElement;
  const blogCard: HTMLElement;
  const activityCard: HTMLElement;
  const homepage: HTMLElement;
}

export {};